// Use map() to double each number in the array: [1, 2, 3, 4, 5].

// arr = [1, 2, 3, 4, 5];

// const add = arr.map(items=> items*2)
// console.log(add)


// ====================================

// Use map() to convert an array of strings to uppercase: ["apple", "banana","cherry"].

// fruits = ["apple", "banana","cherry"];
 
// const Ucase = fruits.map(items => items.toUpperCase());

// console.log(Ucase);
// ==========================================

// Use map() to extract the names from an array of
// const objects = [{name: "Alice"}, {name: "Bob"}, {name: "Charlie"}];

//  const Oname = objects.map(items =>items.name);
//  console.log(Oname)

// =======================================================

// Use map() to return the length of each word in the array: ["dog", "elephant",
// "tiger"].

// const animals = ["dog", "elephant","tiger"];
// const Alength = animals.map(items => items.length);

// console.log(Alength);


// ===============================================/

// Use map() to add a suffix -done to each task in an array: ["task1", "task2",
// "task3"].

// const task = ["task1", "task2","task3"];

// const tDone = task.map(items => `${items} done`);

// console.log(tDone);

// =======================================

// Use map() to square each number in an array: [2, 4, 6, 8].

// const Snum = [2, 4, 6, 8];

// const square = Snum.map(item => item*item);
// console.log(square);


// ============================================

// Use map() to convert an array of prices to strings with a $ sign.

// const prices = [45, 56, 90, 100,25];

// const pDollar = prices.map(item =>`$${item}`);
// console.log([pDollar]);


// ===========================================

// Use map() to create an array of booleans indicating if each number is even.
const numbers = [5, 10, 15, 20, 25];

const num = numbers.map(item => {
    if(item%2===0){
        console.log
    }
}
)
